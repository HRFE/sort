import Sort from './sort.js';

/**
 * 堆排序
 */
class Heap extends Sort{
    constructor(arr) {
        console.log('This is Heap sort:');
        super(arr);
    }

    sort() {
        
    }
}

export default Heap;
